# Vitro 构建指南

> 最后核对：2026-09-27（口径改双轨：本指南构建 Rust 冻结对照 oracle；MoonBit 现役引擎入口见[快速入门](快速入门.md)「零分钟上手」）
> 前一沿革：2026-09-22（防线驱动已全量 Go 化——D5 于 2026-09-18 收官；脚本清单并入
> [脚本总清单与必跑防线.md](脚本总清单与必跑防线.md)）
>
> **双轨口径（2026-09-18 起，同仓绞杀者模式）**：本指南构建的是 **Rust 冻结对照 oracle**（`native/`，tag `rust-oracle-freeze`，只收安全修复，退役前继续可用）；**MoonBit 现役引擎**（`moonbit/`）的构建/闸门/发布流程以 [`moonbit/AGENTS.md`](../../../moonbit/AGENTS.md) 为权威，本指南不重复。
>
> 历史前端构建（Flutter / Android / iOS / web 部署）已随 2026-09-11 前端切割迁出本仓库，
> 对应脚本见标签 `before-frontend-split`；本节只描述**后端引擎与三个出口**的构建。

---

## 环境要求

| 组件 | 版本 | 用途 |
|:---|:---|:---|
| Rust | 1.95.0+ | 引擎核心与三出口（必需） |
| Cargo | 随 Rust 安装 | Rust 包管理（必需） |
| Go | 1.27+（见根 `go.mod`） | **全部测试防线驱动与工具脚本**（D5 后防线均为 `go run ./scripts/<name>`；零第三方依赖，离线可跑） |
| MoonBit 工具链（moon / moonc） | 版本以 `scripts/toolchain_baseline.txt` 锁定 | MoonBit 侧构建 / 测试 / 差分对拍（`moon test` 已是 CI 门禁） |
| Clang / Clang++ | 任意近期版本 | Shadow Verification 的 Golden 生成（防线 1 硬门禁，缺失即 fail fast） |
| Node.js | 任意近期版本 | wasm 出口冒烟（`scripts/wasm_smoke/wasm_smoke.js`） |
| Python | 3.8+ | 仅一次性取证脚本需要（残留清单见[脚本总清单](脚本总清单与必跑防线.md)；不在任何门禁路径上） |
| wasm32 target | `rustup target add wasm32-unknown-unknown` | 出口 2 的 wasm 构建（可选） |

快速检查：

```bash
rustc --version                 # >= 1.95.0
clang --version                 # 需要能直接调用
go version                      # 见根 go.mod
moon version                    # MoonBit 工具链（version 工具链探针锁定基线）
```

---

## 构建

### 1. 引擎核心（cdylib / staticlib / rlib）

```bash
cd native
cargo build                     # Debug
cargo build --release           # Release
```

产物：

| 平台 | 路径 |
|:---|:---|
| Windows | `native/target/release/vitro_native.dll` |
| Linux | `native/target/release/libvitro_native.so` |
| macOS | `native/target/release/libvitro_native.dylib` |

> 这是出口 1（C ABI）的物理形态，头文件在 `native/include/vitro_capi.h`；
> ABI 版本通过 `vitro_abi_version()` 查询（当前 `2.1.0`）。

### 2. CLI 调试工具（出口 3 的入口之一）

```bash
cd native
cargo build --release --bin vitro_cli
```

产物：`native/target/release/vitro_cli.exe`（Windows）/ `vitro_cli`（Linux/macOS）。
命令手册见 [`CLI使用手册.md`](CLI使用手册.md)。

### 3. wasm32 出口（出口 2）

```bash
rustup target add wasm32-unknown-unknown
cd native
cargo build --target wasm32-unknown-unknown --release
```

产物：`native/target/wasm32-unknown-unknown/release/vitro_native.wasm`（**约 3.75 MB**，2026-09-11 冒烟实测：零代码修改即可构建，Node 下 C API 全链路 + E3070 教学诊断均工作）。

体积优化（可选、后置）：`wasm-opt -O --strip-debug`、`opt-level = "z"`、LTO 等；教学场景一次加载后缓存，体积非首要矛盾。

> 已知差异：wasm 下统一模式入口没有 `catch_unwind` 保护，panic 走 abort（详见 [`后端定位与白箱计划.md`](../01-定位与路线 §7）。

---

## 测试与静态检查

```bash
cd native
cargo test --workspace --all-features
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo fmt --check
```

或使用封装的 lint 脚本（clippy + fmt + TODO/FIXME 统计）：

```bash
bash scripts/lint_check.sh
```

基线（2026-09-11 实测）：`845 passed / 0 failed`（60 个测试套件），clippy 0 warning。

---

## 测试防线

### 防线 1：Shadow Verification（C，与 Clang 对照 stdout）

```bash
go run ./scripts/shadow_verify
```

| 选项 | 说明 |
|:---|:---|
| `--jobs N` | 并行度；`0` = 自动，`1` = 串行 |
| `--refresh-clang` | 强制全量重算 Clang Golden（CI 夜间使用） |
| `--rebuild` | release DLL 比引擎源码旧时自动重建 |

**门禁语义（自 2026-09-06 起为 CI 硬门禁）**：Clang 预检缺失 → fail fast（exit 2）；存在非预期差异（compile_gap / runtime_gap / output_gap）→ exit 1；match / known_issue / vitro_better 视为通过。

**产物新鲜度门禁（2026-09-12 新增，两条驱动都生效）**：影子验证与回放读的都是 **release 产物**（`native/target/release/vitro_native.dll` / `vitro_cli.exe`），若产物没跟着源码/提交重建，断言会在"验证陈旧二进制"的情况下**全绿**——这比失败更坏。故：

- `ensure_abi()`（D5 迁移后位于 `scripts/internal/capi`，C/C++ 两侧 Go 驱动共用）除校验 ABI 符号外，还比对 `vitro_engine_version()` 与 `git rev-parse --short HEAD`，不一致即抛错（原 Python 伴生模块 `vitro_output.py` 已随驱动 Go 化退役）；
- `scripts/replay/replay_s1_s5.go`（Go 迁移版，Python 版保留为对照基准）前置门禁：`capabilities.engine_version` 必须含当前 HEAD，否则 **exit 2**；`--anchor` 缺省从版本串自动取（默认值再也无法过期）；
- 提交只改 `.git` 而不动源码，因此 `native/build.rs` 显式声明了 `rerun-if-changed` 指向 `.git/HEAD` 与其指向的 ref —— 否则版本串会停留在上一次改源码的时刻（实测踩坑：HEAD 已 `622a859` 而 dll 仍报 `94c16d2`）。

> 结论：**改完引擎先 `cd native && cargo build --release`**，再跑影子/回放。

**提速设施**：Clang Golden 缓存（key = 源码 + stdin + clang 版本 + 参数 + 预设文件）+ 并行执行。632 用例实测 **103.6s → 1.1s（缓存命中）/ 20.4s（冷启动全量）**（2026-09-11 时点测量记录；用例数现为 671，耗时应按比例外推）。缓存与 worker 目录为 `.clang_cache/` / `.shadow_tmp/`（已 gitignore）——**改动用例后无需手动清缓存**（源码哈希变化自动失效）。

**标准输入**：用例可自带同名 `.in` 文件，Clang 与 Vitro 喂**同一份字节**（缓存 key 纳入真实 stdin）。

> ⚠️ 并行化对顺序敏感：用例加载与分片分发必须确定性（`sorted(glob)` + 按 name 对账），否则会出现"结果错配但门禁仍绿"的静默失败。

### 防线 1'：Shadow Verification（C++）

```bash
go run ./scripts/shadow_verify_cpp
```

### 出口 3 冒烟：serve 协议

```bash
go run ./scripts/serve_smoke
```

覆盖 id 关联 / 帧同构 / 会话生命周期 / 配置一致性 / 三段式内存地图 / schema 轨道与词汇表的 57 项断言（CI 已纳入，脚本自报口径）。

### 防线 5：CI 三层一致性检查

```bash
go run ./scripts/ci_three_tier_check
```

对账 `*_FAILURES.md` 与真实测试结果：已知失败全部转绿 → CI 失败（提示更新文档）；失败记录文件缺失 → CI 失败；测试失败但文档无记录 → WARN。

### 工程健康度看板

```bash
go run ./scripts/engineering_health     # 生成 reports/engineering_health.md
```

统计超大文件、TODO/FIXME/HACK、unwrap/expect、活跃失败记录、Shadow 匹配率。

### 内存安全预检（可选）

```powershell
pwsh scripts/check-memory-safety.ps1
```

扫描 Rust 源码中的常见内存安全反模式；可作为 git pre-commit 钩子安装。

---

## 脚本清单

`scripts/` 全量脚本（CI 门禁驱动 / 差分对拍 / 探针 / 生成器 / Python 残留处置）与
**本地提交前必跑矩阵**已独立成册，见：

**[脚本总清单与必跑防线.md](脚本总清单与必跑防线.md)**（2026-09-22 建册，脚本入册的单一来源；
各脚本用法以自身 `.go` 头注为权威）。

上节"测试防线"仅保留最常用的五条入口命令；MoonBit 侧验证命令另见
[`moonbit/AGENTS.md`](../../../moonbit/AGENTS.md) "构建与验证命令"一节。

---

## 常见问题

### Q1: Shadow Verification 报 "clang not found"

防线 1 以 Clang 为唯一 Golden 来源，**缺失时故意 fail fast（exit 2）而不是静默跳过**。
安装 LLVM/Clang 并确保 `clang`（C++ 用例还需 `clang++`）在 PATH 中。

### Q2: wasm 构建报 "target may not be installed"

```bash
rustup target add wasm32-unknown-unknown
```

### Q3: 构建失败提示 DLL 被占用（Windows）

`vitro_native.dll` 正被 `vitro_cli` 或外部消费者加载。关闭对应进程后重新构建。

### Q4: 改了测试用例但 Shadow 结果没变

缓存按源码哈希自动失效，正常无需干预；若怀疑缓存异常，用 `--refresh-clang` 强制全量重算。

### Q5: 前端（Flutter / Android / iOS / Web）怎么构建？

前端已于 2026-09-11 整体迁出本仓库，本仓库只做后端。
切割前最后完整状态见标签 `before-frontend-split`：

```bash
git checkout before-frontend-split -- CideFlutter
```

### Q6: `cargo test` 与 CI 结果不一致

CI 在 Push/PR 时跑全部防线并执行一致性检查；本地请用同一条命令：
`cargo test --workspace --all-features`，并确认工作目录为 `native/`。
