# vitro/engine 0.1.0（2026-09-19）

> **S1 基础片首发**。溯源口径：本档案补写于 2026-09-23（发布档案制度建立），
> 0.1.x 时代细节以 git 提交与当时执行记录为准。

- **包清单（4）**：`source` / `opcode` / `diag` / `ast`——零依赖基础片（坐标契约、
  132 opcode、137 诊断码 + 77 教学卡片、AST 全族 + depth + E1 emitter）。
- **主题**：诊断契约可用（错误码 → 教学卡片），基础类型层全部就位。
- **发布身份插曲**：首试 403（module owner `vitro` ≠ 当时账号 `jingwei108`）——
  项目所有者注册 `vitro` 账号后解决；命名规则不变（module 名首段 = 发布者名）。
- **验收**：`moon search` 可查 → 全新项目 `moon add vitro/engine@0.1.0` → import
  diag 输出正确；包 zip 根级四件齐全；137 码位 versioned 常量语义随本版占位。
- **版本语义**：minor（首发）。
