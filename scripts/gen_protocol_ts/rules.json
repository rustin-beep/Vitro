{
  "schema": 1,
  "schema_version": "0.1",
  "frozen_anchor": "10591ad",
  "schema_doc": "docs/spec/STEP_PAYLOAD_SCHEMA_V0_1.md",
  "type_sources": [
    "native/src/unified/types.rs",
    "native/src/unified/root_cause.rs",
    "native/src/session.rs"
  ],
  "types": [
    { "name": "AlgorithmStepSnapshot", "kind": "struct" },
    { "name": "ApiVariableSnapshot", "kind": "struct" },
    { "name": "ApiFrameInfo", "kind": "struct" },
    { "name": "AccessedVar", "kind": "struct" },
    { "name": "ArraySnapshot", "kind": "struct" },
    { "name": "PointerSnapshot", "kind": "struct" },
    { "name": "VisEvent", "kind": "struct" },
    { "name": "RootCauseHint", "kind": "struct" },
    { "name": "StepPayload", "kind": "struct" },
    { "name": "PointerStatus", "kind": "enum" }
  ],
  "out_ts": "protocol/index.d.ts",
  "out_meta": "protocol/fields.mjs",
  "_scope_note": [
    "范围 = schema 文档 §1（StepPayload）+ §2（8 个子结构）+ §3.1（PointerStatus 枚举）。",
    "§5 的 StepStreamBatch / StepPayloadDelta **未纳入**：该节表格是压缩式（多字段合并一行，如",
    "`step_index / code_line / func_name_idx / semantic_label_idx`），无法逐字段机判；且它们是",
    "编码细节（差分层），不是内容消费者的必需面。登记为未覆盖，待 v0.2 轨道决定。",
    "权威源选 native/src/unified/*.rs 而非 schema 文档：Rust 结构体是**编译期受字段冻结测试",
    "（native/tests/step_payload_schema_v0_1_test.rs）守护**的真实源；文档是人工维护的表述层——",
    "故取「从实现生成、与文档**双向对账**」：任一方向不同步都判红。"
  ]
}
