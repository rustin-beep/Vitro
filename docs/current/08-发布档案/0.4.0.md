# vitro/engine 0.4.0（2026-09-21）

- **包清单（19）**：新增 `names` / `libc` / `typeck`（S4）+ `codegen` / `bytecode`
  （S5 开工批）+ `parser`（S3，module 级发布随版进架）+ `cmd/{dump_ast,dump_compile,dump_typeck}`
  三工具。至此**编译层全链在架**（前端 → typeck → codegen/bytecode）。
- **主题**：S4 类型检查 4 Pass 全接线（598 语料 E1–E4 归一逐字节一致）+
  S5 codegen 对拍面推进 + **收面 27 符号**（趁未发布窗口用尽前）+
  `moonbit_surface` 双面闸（无主 pub + 消费边）接 CI。
- **版本语义**：minor（新增 8 包 + 公共 API）。
