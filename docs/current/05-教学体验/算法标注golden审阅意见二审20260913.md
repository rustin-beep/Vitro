# 算法标注 golden · 三提交二审（复核 · 2026-09-13）

> **复核对象**：`37445c5`（引擎/serve 层）、`3df2cdc`（步骤判据层）、`e8cfae9`（检测器层 + 人审文档）
> ——即"本人复核驱动"的 U1#1 第三批修复（初审见 `算法标注golden审阅意见.md`）。
> **复核方式**：重建当前工作树二进制 → `serve` + `step.next` 全量提取 82 模板 × 4000 步（34.0s）
> → 与**初审基线**（修复前同一路径的提取）逐条对账 → 门禁实测。
> **判定基线**：初审 100 行逐行判定（62 ✅ / 25 ✗ / 13 ⛔）。
> **边界**：防止回滚麻烦，先分批次进行提交，现重新对提交代码进行审核。本人不是什么高手，审阅仅代表可能存在的问题，不能奉为圭臬，已实例为主，先复现再修复。

---

## 0. 结论摘要

1. **方向正确、主体有效**：初审 38 项缺陷判定中 **18 项完全修复**（含 minIdx 别名、越界守卫、
   初始化行家族、radix 位序、KMP/hanoi 调用点、dp transition 位置）；
   9 项误判**被消除**（四处算法误判清零，且**无附带检测损失**——标注模板集合的变化恰好只有预期的 4 个）。
2. **但有 3 个必须处理项**，其中 2 个是本轮修复**新引入**的：
   - **P0-A｜`step_begin` 未清理 `unified_pending`** → 跨运行复用会话（不调 `session.reset`）时，
     新程序的第一个 `step.next` **下发上一程序的帧**（对照实验已确认因果）。
   - **P0-B｜`binarySearchTreeValidation` 从"静默零标注"变成"按 BST 插入教"**：
     `isValidBST` 被判 `bst_insert`，三条文案挂在 `if (root == NULL) return 1;` /
     `if (root->val <= min || ...) return 0;` / 递归校验行上——**零标注是"没有内容"，错标注是"教错内容"**。
   - **P0-C｜`gcd / mod` 从"数值错"变成"位点错"**：`contains('%')` 过宽，首现漂到 `main` 的
     `printf("%d\n", gcd(a, b));`（格式符 `%`）——原来至少在真语句上。
3. **7 项半修/仍错**：dp 的 `outer_loop` 有 **4 个模板仍挂在初始化循环**上（文案已修、位点没修）；
   `radixSort / count` 仍挂清零行（初审已标，本批未动）；`quick / recursive` 顶层调用仍自称"递归调用…子数组"。
4. **文档与实现对账缺口**：清单 §2 主表 100 行中 **74 行与当前构建一致、16 行描述已变、10 行已失效**；
   另有 **5 行新 phase（dp inner_loop）漏登**。v3 头注写的"实测 36+46"**在写入该头注的同一提交里就已过期**（现为 **34+48**）。
5. 门禁当时实测**全绿**（2026-09-13）：`cargo test --workspace` **68 套件 / 0 失败**、`clippy --all-targets --all-features -D warnings` 零警告、
   `serve_smoke` **57 PASS / 0 FAIL**。（影子验证未复跑，理由见 §7。）
6. **判据锚点覆盖不足**：判据层（`vitro_algorithm_steps`）本批改动约 19 项，**全仓仅有 5 个单测**
   （binary_search ×3、dijkstra ×2），即 17 项判据改动**无锚**——与本项目前几批"每项判据红锚 + 反向锚"的规约不一致。

---

## 1. 可复现命令

```bash
# 修复后全量提取（口径 = 清单 §2：serve step.next，(算法,phase) 首次出现）
python tmp/annot_extract.py --next 4000 --out tmp/annot_extract_after.json
#   基线：tmp/annot_extract_baseline_2121.json（修复前同一路径）
#   归档：reports/annot_extract_20260913_{baseline,after_fix}.json

python tmp/annot_after_diff.py            # 模板级 + 逐条增删对账（-> tmp/annot_pairs_diff.txt）
python tmp/pending_probe.py               # P0-A 复现（跨运行 pending 泄漏）

# 门禁
cd native && cargo test --workspace && cargo clippy --workspace --all-targets --all-features -- -D warnings
go run ./scripts/serve_smoke  # 读 target/debug/vitro_cli.exe（需先 cargo build，debug 曾滞后于修复）
```

---

## 2. P0（必须处理）

### P0-A｜`step_begin` 未清 `unified_pending`：跨运行下发上一程序的帧

**证据（对照实验，`tmp/pending_probe.py` 同款流程）**

```
场景：compile(A=gcd) → run → step.begin → step.next×5 → [可选 session.reset]
      → compile(B=binary) → run → step.begin → step.next×2

无 session.reset：… L12 step=2 / L13 step=3 [a=0,b=0]  ←A 的帧
                  L13 step=4 [a=0,b=0] ←★ B 会话首个 step.next 下发的是 A 的第 5 帧
有 session.reset：… L13 step=3 [a=0,b=0]  ←A 的帧
                  L0 step=0 []           ← B 正常首帧
```

- 根因：`reset_session_preserving_config` 走 `*session = Session::default()`（会清），
  但 **`step_begin`（`session_api.rs`）不碰 `session.unified_pending`**；`compile`/`run` 同样不清。
- 影响：**帧序 +1 错位**且该帧携带**上一程序的 `local_vars`**——教学前端会把旧程序的变量画到新程序上。
  这条通道是 commit `37445c5` 引入的（初审时该字段尚未提交）。
- 建议：在 `step_begin` 开头 `session.unified_pending = None;`（或 `compile`/`run` 一并清），并补一条
  "同会话二次运行不串帧" 的 serve 冒烟断言（`serve_smoke` 现无此形状）。

### P0-B｜`isValidBST` 被按 "BST 插入" 教学（复亮 ≠ 修好）

> **✅ 已修复（2026-09-13，当日）**：采用**方案②**——新增 `bst_validate` 检测 + 步骤模板
>（empty_valid/range_check/recursive 三 phase，实测挂 L20/L21/L32，与下表同挂载点、语义全对）；
> bst 家族判据定清为"名字 + 语义词双条件"，裸命名漏检（bstInsert/bstSearch/bstDelete，
> 即 §3 档 A）由"语义词 + TreeNode 参数语境（`is_treenode_ctx`，整名匹配）"补齐，三模板
> 0→4/7/11 条复亮；`bst_delete` 新算法同步落地。顺带实锤并修复两处既有缺陷：`extract_features`
> 曾给 walk 传空函数名、且 `Expr::CallPtr` 形态调用不被 walk 匹配——`is_recursive` 在真实
> 管线恒 false（bstSearch 的 search 函数 47 帧零标注的叠加根因）。82 模板对账：受影响集合
> 恰好 = {BSTV, bstInsert, bstSearch, bstDelete}（303→325 条）。红→绿锚：
> `isvalidbst_detected_as_bst_validate_not_insert`（修复前 FAIL 留痕）+
> `native/tests/algorithm_detector_pipeline_test.rs` 管线锚。详见 CHANGELOG [Unreleased]。

`e8cfae9` 的 `has_word` 缩写合并让 `binarySearchTreeValidation` 从 0 条标注恢复到 39 帧，但**判据本身过宽**：

```rust
// tree.rs：名字含 bst 整词 → 直接判 bst_insert
if has_word(func_name, "bst") { matches.push(build_match("bst_insert", …)); }
```

实测首现（三条全部与所挂语句语义不符）：

| 挂载行 | 源码 | 标注 | 问题 |
|--------|------|------|------|
| L20 | `if (root == NULL) return 1;` | 找到空位，创建新节点 | 校验函数**不创建节点** |
| L21 | `if (root->val <= min \|\| root->val >= max) return 0;` | 比较插入值与当前节点值，决定向左或向右 | 是**区间校验**，无"插入值"、无左右决策 |
| L32 | `return isValidBST(root->left, min, root->val) &&` | 递归查找插入位置 | 是**递归校验** |

- 同时暴露同类风险：任何名字含 `bst` 的函数（`bstHeight`/`bstTraverse`/`bstValidate`）都会被讲成"插入"。
- 建议（二选一，需裁定）：
  ① 判据加"插入语义词"要求（`insert`/`add`/`create` 整词或 `return *root = …` 形态）——`isValidBST` 落零标注；
  ② 新增 `bst_validate` 检测 + 步骤模板（成本高，但教学价值真实存在）。
  **不建议**保留现状——初审的 ⛔ 判定依然成立，只是错法从"没有"换成"教错"。
- 注意：这条与 §3 档 A 的"bstInsert/bstSearch/bstDelete 裸命名漏检"是**同一判据的两侧**，
  建议一次性定清"bst 家族判据"（名字 + 语义双条件）。

### P0-C｜`gcd / mod` 位点漂到 printf 行（`contains('%')` 过宽）

修复把文案降级为不展示操作数（方向正确，且"prev 操作数管道登记下批"已在 CHANGELOG 声明），
但**判据同步放宽**了：`line_lower.contains("a % b") || contains("mod")` → **`contains('%')`**。

```
实测 gcd：mod 首现 = L14  printf("%d\n", gcd(a, b));   ← 格式符 '%' 命中
          （真实语句 L6 `b = a % b;` 仍在，但已不是首现）
```

- 影响：学生在新程序入口看到的第一个"求余"提示挂在一句 `printf` 上。
- 建议：`contains('%') && !contains("printf")` 或恢复 `a % b` / `%=` 形态判据（两条都要能覆盖 `b = a % b`）。

---

## 3. P1（应处理）

| # | 项 | 现状（实测） | 建议 |
|---|----|--------------|------|
| P1-1 | **dp `outer_loop` 仍挂初始化循环** | `dpCoinChange` L7 `for (i<=amount) dp[i]=100000;`、`dpLIS` L7 `for (i<n) dp[i]=1;`、`dpLCS` L10、`matrixChain` L5 —— 4/6 仍错位（文案已修 ✅） | 外循环判据加"非初始化体"条件（循环体为 `dp[…] = <字面量>` 时不算） |
| P1-2 | `radixSort / count` 仍挂清零行 | L12 `for (i<10) count[i] = 0;`（初审 P1-80 已标，本批只改了 `digit_loop`） | 与 `countingSort / place` 同款收紧（排除右侧字面量 0） |
| P1-3 | `quick / recursive` 顶层仍自称"递归调用…子数组" | L31 `quickSort(arr, 0, n - 1);` —— 错字已修 ✅、空区间已屏蔽 ✅、顶层未区分 ❌ | 与 `merge` 同样的"顶层 vs 递归"区分；注意 `merge` 的实现是死代码（见 P1-5） |
| P1-4 | **`merge` 的"启动归并"分支是死代码** | 全量提取中"启动归并"出现 **0 次**；判据为 `source_line.contains("main")`，而 `mergeSort(arr, 0, n-1);` 不含 "main" → 永假 | 用"当前帧是否位于 main 的调用点"判定（如 `at_callee_entry`/caller 信息），而非源码行含 "main" |
| P1-5 | **函数签名行也被标注**（4 处） | `hanoi` L3 `void hanoi(int n, …)` → "移动 2/1 个盘子的汉诺塔问题"（本批新引入）；`computeNextVal`/`stringMatchKMP` L4 `void getNext(char T[], int next[]) {` → "构建 next 数组，next[-1]=-1"（`next[]` 含 `next[`） | 判据排除"函数定义行"（`code_line == func.loc.line`），或对签名行给"进入函数"类文案 |
| P1-6 | **零标注缺口登记缺失** | 收紧致 3 个模板转零标注（`activitySelection` / `externalSort` / `mergeSortedLists`），`huffmanTree` 从 10 条降到 1 条（只剩正确的 `merge`） | §3 应登记这 3 个新缺口（外部排序/置换选择/链表归并是真实教学内容，判"零标注"是代价而非成果）；`huffmanTree` 的 `select` 分支仍零触发 |

---

## 4. 文档与实现对账（清单需重提取，且 v3 头注自身已过期）

### 4.1 §2 主表 vs 当前构建：**74 一致 / 16 描述已变 / 10 行失效**

**10 行失效**（模板已零标注或该 phase 不存在）：

| 行 | 模板 | phase | 失效原因 |
|----|------|-------|----------|
| 1、2 | activitySelection | compare / inner_loop | 误判消除 → 模板零标注 |
| 40、41 | externalSort | compare / outer_loop | 同上 |
| 56、57 | huffmanTree | compare / outer_loop | 该 phase 不再产出（仅剩 merge） |
| 73–75 | mergeSortedLists | finish / merge / recursive_split | 误判消除 → 模板零标注 |
| 93 | stringBasicOps | recursive | 上批已失效，本批未删 |

**16 行描述已变**（2 类）：

- **文案泛化/收紧**（10 行）：`bfs/enqueue` 起点入队、`computeNextVal`+`stringMatchKMP` 的 `build_next` 调用文案、
  `gcd/mod`、`hanoi/recursive`、`insertion/insert`、`primMST/add_vertex`、`quick/recursive`（错字）、
  `selection/compare`+`inner_loop`（min_idx=0 / arr[0]）
- **dp 文案泛化**（6 行）：`dpCoinChange` / `dpFib` / `dpKnapsack` / `dpLCS` / `dpLIS` / `matrixChain` 的
  `outer_loop` → "遍历子问题 i="

> 注：`radixSort/digit_loop` 的**首现文案未变**（exp=1 → "第 1 位"），变化的"第 2 位/第 3 位"属同 phase 的后续条目，
> 故不在本表 16 行内——逐条增删请以 `tmp/annot_pairs_diff.txt` 为准。

> 精确的逐行对应见 `tmp/annot_pairs_diff.txt`（237 行）与 `reports/annot_extract_20260913_after_fix.json`。

### 4.2 漏登 5 行（新 phase）

`dpCoinChange` / `dpKnapsack` / `dpLCS` / `dpLIS` / `matrixChain` 的 **`inner_loop`**（"遍历子问题维度 j="）
——本批由 `i_head` 收紧**新产生**的 phase，清单主表无对应行。

**v3 主表口径建议**：**34 模板 × 95 条**（现 38 × 100）。

### 4.3 §2.1 / §3 / §4 与 v3 头注

| 位置 | 问题 |
|------|------|
| v3 头注 | 写"实测 **36+46**"，而**同一提交自身的收紧**已使其变为 **34+48**（3 个模板转零、BSTV 复亮）→ 头注即过期 |
| §2.1 | 表内 5 行（activitySelection/externalSort/huffmanTree/mergeSortedLists）**已整模板零标注**，行已无对象；"其余 93 条"算术问题（7 行 vs 9 条）仍在；"性质"列的"变量取不到值"归因未改（其中三行实为算法误判） |
| §3 | 标题仍"44 个零标注"、口径仍 `38+44`；档 A 仍 4 个（**应含 BSTV 的判据侧问题**）；档 C 仍含 `bTree`/`criticalPath`（与 §4"截断"名单冲突——实测二者零标注，支持归 C） |
| §4 | "截断 7 个"仍未验证（见 §7）；"4 个已在 §2 出现行"表述在重提取后需同步 |

---

## 5. 初审 38 项逐项处置（二审结果）

| 处置 | 数量 | 项 |
|------|------|-----|
| **完全修复** | **18** | 6 bfs/enqueue、19 computeNextVal、21 countingSort/place、29 dpCoinChange/transition、31 dpFib/outer_loop（文案）、32 dpFib/transition、34 dpKnapsack/transition、36 dpLCS/transition、39 dpLIS/transition、50 hanoi/recursive（调用点）、60 insertion/insert、70 matrixChain/transition、76 primMST/add_vertex、81 radixSort/digit_loop、84 selection/compare、85 selection/inner_loop、89 seqList/update_len、96 stringMatchKMP/build_next |
| **误判被消除（代价=零标注，需登记）** | **9** | 1、2 activitySelection；40、41 externalSort；56、57 huffmanTree；73–75 mergeSortedLists |
| **半修/仍错** | **7** | 28 dpCoinChange/outer_loop（位点）、35 dpLCS/outer_loop（位点）、38 dpLIS/outer_loop（位点）、69 matrixChain/outer_loop（位点）、46 gcd/mod（换错法）、79 quick/recursive（顶层）、80 radixSort/count（未动） |
| **转移为新错** | **3** | 12–14 binarySearchTreeValidation（失效 → 错算法标注，见 P0-B） |
| **未处理** | **1** | 93 stringBasicOps（行未删、§3 未登记） |

**新引入问题（初审清单外）**：P0-A、P0-C、P1-4、P1-5，以及 P0-B 的暴露。

---

## 6. 门禁实测

| 门禁 | 结果 | 与 CHANGELOG 声明 |
|------|------|-------------------|
| `cargo test --workspace` | **当时 68 套件 / 0 失败**（含本批新增检测器锚 13 个、判据锚 5 个） | 一致（"cargo 68 套件"） |
| `clippy --workspace --all-targets --all-features -- -D warnings` | **零警告** | 一致 |
| `serve_smoke.py` | **断言 57 / PASS 57 / FAIL 0** | 一致 |
| 影子验证 C/C++ | 未复跑 | 声明 671 / 非预期 0 |

**未复跑影子的理由**：三提交只动检测器/步骤标注/`session_api` 缓冲，未触及词法→VM 任何语义路径；
影子验证走 capi（`vitro_native.dll`），与本批改动无交集。如需补跑，请让我执行（release 已是修复后构建）。

**锚点覆盖**：`vitro_algorithm_steps` 全 crate **仅 5 个测试**（binary_search ×3 + dijkstra ×2），
本批改动的 dp（3 项）/ gcd / hanoi / seqList / bfs / primMST / selection（2 项）/ insertion / quick / merge /
counting / radix / KMP **均无锚**。建议按前几批规约补齐"红锚 + 反向锚"（尤其 P0-C 与 P1-1/P1-2 三条已知会漂的判据）。

---

## 7. 未复核 / 待裁定

1. **§4"截断 7 个"无法验证**：4000 步内所有模板都持续返回帧且 `err_count = 0`，从帧数区分不出"未跑完"与"已结束"；
   需要 serve 侧补"程序结束"信号（或让 `step.next` 在结束后返回终止帧）。
2. **`payload.get` 与 `step.next` 对同一帧的时序差异**：缓冲帧先入 `frame_cache`（`payload.get` 可见、标注可能尚未被回改），
   下一帧到达后才在缓存内被清除。清单 §2 已声明"勿混用两条路径"，但**缓存被溯及改写**这一点建议在协议文档写明。
3. **`step_next` 用 `result.payloads = vec![pending]` 整体替换**：当前 `run_batch(…, 1)` 恒 ≤1 帧故无影响，
   但一旦有人把 batch_size 调大就会**静默丢帧**——建议加断言或改为 append。
4. **`has_word` 等价性**：我手算核对了 `isValidBST`/`BSTInsert`/`getHTTPResponse`/`subString`/`bestEffort` 五形（结果与实现一致），
   但未穷举；建议补一个"大写段末归属"的表格化单测（`BSTInsert` 的 I 归下词这一条已锚）。
5. **`hanoi` 仍产出"递归移动 0 个盘子"**（n=1 不应进入递归行）——低危，需核帧序。
6. **`matrixChain` 的 `transition` 挂在 `int cost = dp[i][k] + …`**（成本计算行）而非赋值行——可接受，但 v3 主表需按实测记录。

---

## 8. 值得肯定的部分（简短）

- **四处算法误判清零且零附带损失**：受影响模板集合精确等于预期（−activitySelection/externalSort/mergeSortedLists，+BSTV），
  说明"命名主导 + 语境排除"的收紧方向对，且没有再引发第三轮误判。
- **dp 三个判据一次到位**：`transition`（双侧 `dp[`）在 **6/6** 个 dp 模板上落到真正的转移行，`inner_loop` 新增 5 个模板覆盖。
- **"初始化行家族"修复彻底**：`primMST add_vertex`（→ L24）、`countingSort place`（→ L11）、`seqList update_len`（→ L26）、
  `selection minIdx`+越界守卫，实测描述中 `?` 与越界下标均已消失。
- **`has_word` 缩写合并实现正确**（边界规则"大写→大写且下个小写则分词"），双向锚齐（`isValidBST` 识别 / `subString` 不识别）。
- **门禁全绿且 release 已重建**（21:53）——不是"验证陈旧二进制"。
