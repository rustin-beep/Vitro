# Build outputs
/build
/out
/.vs

# Native build directories
/native/build
/native/build-android*
/native/target

# Rust build logs
native/check.log

# .NET build artifacts (all platforms)

# Distribution / publish outputs
/dist

# Temporary build artifacts that should never be in the repo root
/*.exe
/*.o
/*.obj
/*.wasm
/*.wat
/*.ilk
/*.pdb
/*.lib
/*.dll
*.so
/*.tmp
*.log

# IDE / editor
*.user
*.suo
**/.vs/
.vscode/settings.json
.idea/
**/Properties/launchSettings.json

# OS
.DS_Store
Thumbs.db

# Test & coverage
TestResults/
coverage/
*.coverage
*.coveragexml

# Shadow verification framework (auto-generated reports & temp files)
native/tests/shadow_verification/reports/
native/tests/shadow_verification/test.txt
/test.txt
/numbers.txt
/lines.txt
/out.txt
!native/tests/shadow_verification/reports/SHADOW_VERIFICATION_REPORT_*.md
!native/tests/shadow_verification/reports/.gitkeep

# Three tier test reports (auto-generated by CI)
reports/
!reports/.gitkeep

# Template sync & e2e auto-generated artifacts
native/tests/cases_golden/golden_failures.log
native/tests/TEST_REPORT.md

# Build logs & artifacts
*.binlog
artifacts/

# Package manager
*.nupkg
packages/
.tmp-monaco/

# 根目录探针/对照产物工作区（会话探针与根目录 clang 对照 exe；
# shadow 驱动自管目录在 native/tests/shadow_verification/ 下，另有规则）
.shadow_tmp/
node_modules/

# Blazor / static web assets generated from NuGet packages
**/wwwroot/_content/

# Build outputs

# Python
*.pyc
**/__pycache__/
scripts/__pycache__/
native/tests/test_detect.*
native/test_*_c_clang.exe
native/tests/bytecode_libc_consistency/*.exe
native/examples/repro*.rs

# 补充：防止构建产物再次进入历史
# Android CMake 生成的编译器检测文件
**/CMakeFiles/**/CMakeCXXCompilerId.cpp
**/CMakeFiles/**/CMakeCCompilerId.c

# 第三方库 map 文件（体积大且非必须）
**/*.css.map
**/*.js.map

# 字体文件（如果很大且可外部获取）
# 注意：保留 .ttf 跟踪，因为可能是自定义字体
# 如果是标准开源字体（如 OpenSans），建议改为 CDN 引用

# Rust 增量编译产物（保险起见）
native/target/
**/*.rmeta
**/*.timestamp

#ai
.workbuddy

# Shadow 提速设施（2026-09-11）：Clang 结果缓存 + worker 隔离运行目录
native/tests/shadow_verification/.clang_cache/
.clang_cache_cd/
native/tests/shadow_verification/.shadow_tmp/
# C++ shadow 驱动的自管工作目录（替代 tempfile，E-P1-5 同期修复）
/.shadow_cpp_tmp/

# 临时探针/复现脚本（2026-09-11 清理轮起明确忽略）：
# 审查复核与修复验证用的探针统一放这里，禁止入库（历史上有 56 个误入，已清除）
/tmp/
tmp_threaded*.exe
# 探针临时目录（E 系列 batch 探针，勿提交）
.e2_probe/
.e3_probe/

# 诊断临时文件（根目录散落的临时副本仍忽略；常设工具已转正 scripts/diag_mem.ps1）
/diag_mem.ps1

# 随机差分探针（random_diff）的编译工作目录与最小复现产物
scripts/core_asset_verdict/.randomdiff/
scripts/core_asset_verdict/.findings/
cargo_test_ci.log

# MoonBit 构建产物（moonbit/ 活跃区）
moonbit/_build/
moonbit/scripts/_build/
moonbit/.mooncakes/
scripts/lexer_diff/_build/
moonbit/target/
moonbit/.repos/
