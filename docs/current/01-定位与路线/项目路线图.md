# Vitro 路线图

> 最后核对：2026-09-23（MoonBit 迁移现状对齐——状态表刷新、进行中改 S 系列视图、capi 后续批次处置落档）
>
> **核心原则**：不急着发布，不把时间浪费在"能用"上；每一分投入都指向教学场景真正需要、而通用工具链不提供的能力。
> **当前定位**：教学 C 子集参考执行引擎（白箱后端，MIT；迁移 v1 = C only，C++ 已裁砍冻结于 oracle）。前端已切割给社区，原生移动端放弃。
> **当前工作**：**MoonBit 迁移（同仓绞杀者模式）**——Rust 版冻结为差分对照 oracle，`moonbit/` 活跃区承载全部新开发；排期权威 = [MoonBit迁移总计划.md](MoonBit迁移总计划.md) §10 S 系列。

---

## 一、当前状态（2026-09-23）

| 维度 | 状态 |
|:---|:---|
| 引擎核心（现役 oracle） | Rust workspace（10 个子 crate）已**冻结**（tag `rust-oracle-freeze`，只收安全修复），继续承担 shadow 对拍与教学场景现役 |
| **MoonBit 迁移** | 🚧 **进行中**：S2 lexer / S3 parser / S4 typeck / S5 codegen+bytecode 已收官（A 级对拍 598/598 全闭环）；S6 memory+host 已落、vm 片推进中（批一号状态定形 + 批二段一 executor 骨架——opcode 135 / VMSnapshot / 八族；executor 131/135 臂已接，CallHost 110 臂全接线，余 C# 三件〔CS 批〕 + 回调族 9）；`moon test` 423 用例全绿；mooncakes `vitro/engine` 已发布 0.5.0 |
| 出口 1 · C ABI | ✅ 可用（Rust 冻结区）；第一批 13 个入口全部落地，`vitro_abi_version()` = 2.1.0；**后续批次已裁不做**（下游改道 wasm/serve，见 [wasm多实例并发模型与U2拍板](../06-出口与协议/wasm多实例并发模型与U2拍板.md)） |
| 出口 2 · wasm | Rust wasm32 冒烟实证在案；**目标出口已改为 wasm-gc 单出口多宿主**（MoonBit 侧 S7 gateway 片） |
| 出口 3 · serve | ✅ `vitro_cli serve` JSON-lines 落地（Rust 冻结区现役）；MoonBit 侧协议/会话片排 S7 |
| 协议 | ✅ StepPayload schema v0.1 **已冻结**（签字回放 61/61，facts `replay_assertions` 机判；TS 类型生成链 @vitro/protocol 已接线） |
| 测试防线 | ✅ C Shadow 683 用例、C++ Shadow 99 用例（随砍 C++ 跑到 Rust 区退役）、Rust cargo test 1029 用例 / 64 套件（2026-09-23 实测）、MoonBit 423 用例 + 十三闸（2026-09-26 实测） |
| C 子集 | ✅ 教学子集 + C99/C11 扩展（VLA / `_Generic` / 复合字面量 / 变参 / VFS 文件 I/O 等）——迁移主战场，逐片对拍中 |
| C++ 子集 | ⛔ **已裁砍（2026-09-20）**：MoonBit 侧零迁移，Rust 冻结区语义参考保留（规范已归档，见文档地图） |
| 统一模式 | ✅ Rust 侧全链路可用；MoonBit 侧时间旅行/教学智能排 S8（性能优化路线已实测裁定：checkpoint+写集 undo 替代每步全量快照，见[性能探究实录](../07-质量与裁定/20260922_性能探究实录.md)） |
| 前端 | ⛔ 已迁出（标签 `before-frontend-split`） |

---

## 二、已完成里程碑

### 引擎与语言（Stage 0~8）

| 阶段 | 内容 | 状态 |
|:---|:---|:---|
| Stage 0 | 基础编译器（Lexer → Parser → AST → TypeChecker） | ✅ |
| Stage 1 | 自研 VitroVM（字节码定义 + 解释器 + C API 接线 + 安全加固） | ✅ |
| Stage 2 | 运行时中文诊断（符号表导出 / 越界 / 除零 / 空指针 / 死循环分析） | ✅ |
| Stage 3 | 指令级单步 + 内存视图 + 指针追踪 | ✅ |
| Stage 4 | 零侵入可视化（算法识别骨架 + VM 运行时教学事件 + 运行时验证） | ✅ |
| Stage 5 | 诊断与修复系统（三级信息架构 + 结构化自动修复 + 知识卡片） | ✅ |
| Stage 6 | ~~前端搭建（Flutter + 自研编辑器）~~ → **已随前端切割迁出** | ⛔ |
| Stage 7 | C 子集拓展（`float`/位运算/三目/指针算术/`const`/`NULL`/函数指针等） | ✅ |
| Stage 8 | 统一模式 / 时间旅行（快照 + 检查点 + Seek + 自动执行 + 异常回退） | ✅ |

### 工程化（Phase 0~42，逐项记录见 [AGENTS.md](../../../AGENTS.md) 与 [CHANGELOG.md](../../../CHANGELOG.md)）

- **Phase 18~26**：地毯式审阅（P0 soundness 修复、VM 优化、clippy 0 警告）、UAF/Double-Free 检测、认知推理 P0~P3（轨迹根因 / 误区模式 / 知识图谱 / 意图推断）、语义补全 v2、模板 JIT
- **Phase 27~30**：数据结构语法拓展（数组退化 / `unsigned` / `const` / `extern` / VLA）、CLI 工具、Bytecode Libc 产品化、语法拓展（逗号运算符 / Designated Initializer / `offsetof`）
- **Phase 31~42**：C++ 教学子集（类/继承/模板单态化 → RAII → `new[]`·`delete[]` → 引用 → 隐式移动构造 → `unique_ptr` → 内置容器收口与布局解耦 → M6 测试防线 → **Phase 42 进行中**）
- **2026-09-06**：全面代码审阅（137 条发现）+ 四批修复；Shadow 与 CI 门禁"带牙齿"
- **2026-09-11**：**前端切割**（仓库转型纯后端）；capi 第一批；`vitro_cli serve`；StepPayload schema v0.1；堆内存 bump + 有界隔离决议；Shadow 提速（103.6s → 1.1s 缓存命中）与 stdin 注入
- **2026-09-18**：**MoonBit 迁移总计划定稿**（四道证伪门全关 + 包切分 L0–L9 + S 系列里程碑）；同日起实际排期权威从 U 系列让位于总计划 S 系列
- **2026-09-19**：S0.5 Rust 止血批 + S1 基础片 + S2 lexer 收官（`vitro/engine` 首发 mooncakes）；S3 parser 收官
- **2026-09-20**：S4 typeck 收官（598 语料 E1–E4 全绿）；**砍 C++ 裁定落档**（总计划 F-2 + C# 计划 v4）
- **2026-09-21**：S5 codegen 收尾（A 级对拍 598/598 全闭环）+ 0.4.0 发布；架构审阅报告 v1/v2 入库
- **2026-09-22**：性能探究实录四轮（时间旅行累赘实锤 + wasm 生成器路线裁定）；@vitro/protocol TS 类型生成链；四个跨语言一致性闸门
- **2026-09-23**：**0.5.0 发布**（parser 随架）；S6 memory + host 建包与余量三批（110 路由 handler 全接线含 VFS 17 件）；CI 拆 core/hygiene 两 job

---

## 三、进行中与下一步

> **排期权威已移交**（2026-09-18 起）：实际执行以 [MoonBit迁移总计划.md](MoonBit迁移总计划.md) §10 S 系列为准，本节为执行视图摘要。前端切割时代的 Phase 2a/2b/3 计划处置如下：Phase 2a wasm 白箱 → 目标出口改为 MoonBit wasm-gc 单出口（S7 gateway 片）；Phase 2b/3 capi 第二三批 → **已裁不做**（[wasm多实例并发模型与U2拍板](../06-出口与协议/wasm多实例并发模型与U2拍板.md)：19 声明冻结现状，下游改道 wasm/serve）；Phase 3 时间旅行 CoW → 性能路线已实测裁定（checkpoint+写集 undo 替代每步全量快照，[性能探究实录](../07-质量与裁定/20260922_性能探究实录.md)③；MoonBit 侧"Trap 回退改检查点+正向重放"已列入按目标架构实现清单）。

### S6 · memory + host + vm 🚧 进行中（2026-09-23 开工）

- ✅ `vitro/engine/memory` 建包（1MB 载体 + MemoryMap 单出口 + FreedLogs 有序数组，27 测试）
- ✅ `vitro/engine/host` 建包 + 余量三批（路由表单源落 bytecode 包、输出通道 Bytes 化、printf/scanf 引擎、VFS 17 handler——110 路由中 104 测试全绿）
- ⏳ **vm 片未开工**（executor + snapshot；host 控制流/回调族 9 件——exit/abort/qsort 等——随 vm 片）
- 验收锚：D 级 30 例三联 diff + 门 3 集成版 + 条件 A 性能锚（总计划 §2）

### S7–S9 与全量切换（未开工）

| 片 | 内容 |
|:---|:---|
| S7 | session / protocol / gateway + Node 宿主（协议帧双宿主对拍 + replay/serve_smoke 重建 + wasm-gc 产物） |
| S8 | time_travel / teaching / analysis / diagnostics + 差异台账 CI |
| S9 | 裁定批：JIT 复核 / libc 机制形态 / Wasmtime 形态（C++ 已裁砍出列） |
| 全量切换 | shadow 逐项一致（match/known_issue/gap 三口径）→ **Rust 区整体删除**（档案 = tag `rust-oracle-freeze` + git 历史） |

### 持续项

- **文档同步**：实现变更必须同步 `C语言子集规范.md` / `CHANGELOG.md` / 相关 AGENTS.md（MoonBit 侧以 moonbit/AGENTS.md 包清单为状态权威）；
- **防线维护**：`*_FAILURES.md` 双向对账（转绿未更新文档即 CI 失败）；
- **facts 对账**：CURRENT 文档裸数字与 `reports/facts.json` 真值机判（`go run ./scripts/facts check`）。

---

## 四、已知缺口（诚实记录）

| # | 缺口 | 影响 | 处置 |
|:---|:---|:---|:---|
| G1 | ~~模板 → 用例生成器缺失~~ | 链路断裂 | ✅ 已恢复（重构批次 R4，2026-09-12）：`scripts/sync_templates.py` 自历史提交恢复并去除 Flutter 输出口径 |
| G2 | ~~wasm 冒烟脚本未固化~~ | wasm 出口缺 CI 守护 | ✅ 已固化（重构批次 R4，2026-09-12）：`scripts/wasm_smoke/wasm_smoke.js` + ci.yml wasm32 构建/冒烟步骤 |
| G3 | wasm 下统一模式无 `catch_unwind`（panic → abort） | 与原生出口行为有差异 | 已记录，评估中 |
| G4 | `time()` / `clock()` 使用真实墙钟，破坏重放确定性 | 时间旅行回放可能不一致 | Phase 3 伪时钟 |
| G5 | `fprintf` 到自定义 `FILE*` 不落盘（写入被当作 stdout） | 与 Clang 不一致 | 已记录；教学场景请用 `fputs`/`fwrite`/`fputc` |
| G6 | ~~`native/src/flutter_bridge.rs` 命名与定位残留~~ | 历史包装层，命名易误解 | ✅ 已收敛（重构批次 R2，2026-09-11）：`flutter_bridge.rs` 整删，vitro_cli 全部子命令直用本地 `Session` + `session_api`，全局会话单例（u64 map + static）清零 |
| G7 | ~~StepPayload schema v0.1 尚未完成对端回放校验~~ | 协议冻结未闭环 | ✅ 已闭环：签字回放 61/61（`scripts/replay/replay_s1_s5.go`，facts `replay_assertions` 机判；2026-09-21 架构审阅复核确认冻结成立） |
| G8 | 通用解释器路径仍有性能优化空间（时间旅行每步全量快照） | 10 万步级程序 seek 延迟 | 性能路线已实测裁定（2026-09-22）：checkpoint 重放 + 写集 undo 替代每步全量快照，MoonBit 侧随 S8 落地（[性能探究实录](../07-质量与裁定/20260922_性能探究实录.md)③）；Rust 侧冻结不修 |
| G9 | **算法属性验证能力在后端从未落地**：`validate_algorithm()` / `ValidationResult` 在 `native/` 全目录零命中（原 `native/src/engine/algorithm_validator.rs` 不存在），原载体 `CideFlutter/lib/models/algorithm_validation.dart` 已随前端迁出；`LearningProgress` 进度追踪同理（后端仅有 `learning_path.rs` 的 `LearningPath`）。**澄清**：`AlgorithmMatch`（算法检测结果结构体）在 `native/src/session.rs` 确实存在，缺的是"运行时验证"环节本身 | 零侵入可视化的"运行时验证"维度缺后端支撑 | 待评估重建（2026-09-11 翻新时由文档核对发现，详见 [算法与数据结构教学设计.md](../05-教学体验/算法与数据结构教学设计.md) §7） |
| G10 | ~~C++ E2E 用例口径不一致~~ | 文档数字与实际断言不一致 | ✅ 已对账（重构批次 R4，2026-09-12）：CPP_FAILURES.md 更新为 78 |
| G11 | ~~工程债务回升~~（D14 unwrap×3 / D15 health 未进 CI / D16 decl.rs 超标） | 与"生产代码 0 unwrap"的历史验收标准不符 | ✅ 已收口（重构批次 R4，2026-09-12）：unwrap 归零、engineering_health 进 CI、decl.rs 792 行 + decl_types.rs 拆分 |
| G12 | ~~模板失败计数存在三套口径~~ | CI 双向对账存在盲点 | ✅ 已统一（重构批次 R4，2026-09-12）：实测 `infixEvaluation_default` 已通过并从失败口径移除；AGENTS 口径更正为 82/80 绿/2 已知失败，与常量一致 |
| G13 | ~~两条 C++ 活约束未记入 `C++子集规范.md`~~ | 学生可见边界缺权威记录 | ✅ 已补记（重构批次 R4，2026-09-12）：C++子集规范.md 新增"模板活约束"小节 |

> 其它已知语言子集差异见 [C语言子集规范.md](../03-语言子集/C语言子集规范.md)（C++ 差异随规范归档，见 [ARCHIVE_C++子集规范.md](../../archive/ARCHIVE_C++子集规范.md)），
> 测试差异见 `native/tests/*_FAILURES.md`。

---

## 五、差异化能力（相对通用工具链）

1. **运行时中文教学诊断**：GDB 只说 `SIGSEGV`，OnlineGDB 只说 `Runtime Error`；Vitro 说"你访问了 `arr[5]`，但数组只有 5 个元素，有效索引 0~4；发生在第 3 行；当前 `i = 5`"，并给出一键修复。
2. **零侵入算法可视化**：写纯 C 冒泡排序，引擎自动识别并给出每一步的比较/交换语义与数组快照，无需任何可视化 API。
3. **内存与指针白箱**：值级变量快照、指针四状态（Valid / Freed / Null / Dangling）、堆区域与泄漏报告、隔离区可视化。
4. **时间旅行回放**：任意回退、异常自动回退到上一步并给根因卡片——这是"程序不是黑箱"的直接教学载体。
5. **协议化交付**：三出口 + 语言中立 schema，任何前端/IDE/判分服务都能接入，而不必绑定某一种 UI 技术栈。

> 单步/回放体验的设计论证见 [VM教学体验优势.md](../05-教学体验/VM教学体验优势.md)。

---

## 六、文档地图

| 想知道什么 | 看哪里 |
|:---|:---|
| **MoonBit 迁移（当前工作）** | [MoonBit迁移总计划.md](MoonBit迁移总计划.md)（排期权威 §10）+ [第一阶段计划](MoonBit迁移第一阶段计划.md) + [活跃区操作手册](../../../moonbit/AGENTS.md) |
| 后端定位与切割决策 | [后端定位与白箱计划.md](后端定位与白箱计划.md) |
| 架构总纲 | [架构设计.md](架构设计.md) |
| 构建与测试防线 | [构建指南.md](../02-构建与上手/构建指南.md)、[快速入门.md](../02-构建与上手/快速入门.md) |
| CLI 命令 | [CLI使用手册.md](../02-构建与上手/CLI使用手册.md) |
| 语言子集契约 | [C语言子集规范.md](../03-语言子集/C语言子集规范.md)（C++ 规范已归档：[ARCHIVE_C++子集规范.md](../../archive/ARCHIVE_C++子集规范.md)） |
| 标准库支持矩阵 | [标准库支持矩阵.md](../04-标准库与防线/标准库支持矩阵.md) |
| 协议 schema | [STEP_PAYLOAD_SCHEMA_V0_1.md](../../spec/STEP_PAYLOAD_SCHEMA_V0_1.md) |
| 审查与修复追踪 | [代码审阅与修复追踪20260906.md](../07-质量与裁定/代码审阅与修复追踪20260906.md)（0911 复核已闭环，归档见 [ARCHIVE_代码审查复核20260911.md](../../archive/ARCHIVE_代码审查复核20260911.md)） |
| 工程维护 | [工程债务维护方案.md](工程债务维护方案.md) |
| 历史文档 | [../../archive/](../../archive/)（仅供追溯，可能严重过时） |
