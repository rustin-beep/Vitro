# vitro/engine 0.5.0（2026-09-23）

- **包清单（19）**：与 0.4.0 相同（无新包）。
- **版本语义裁定：0.4.0 → 0.5.0（非 patch）**——接口面三处实变：codegen 新增
  `pub fn compile_library`（library mode 出口）、libc 新增 `LibcSig`、ast 收面
  移除 `template_arg_eq`（唯一消费者同包）。按手册「新增公共 API → minor」。
- **主题**：**README 英文化**（English 节 + 11 包英文表 + 稳定性承诺）——
  mooncakes 模块页国际可读；`description` 升级完整英文句、`keywords` 增
  `moonbit`；parser「随下一 minor」承诺兑现对齐。
- **验收三件**：`moon search` 可查 / 临时项目 `moon add` 编译运行全对 / 包 zip
  根级四件齐全；发布前置全绿（moon check 0 错、210/210、九闸 + toolchain_probe）。
- **外部用户证实**：下载数 25 > 自产上限 ~18（两人三倍验证口径）——**此后对外
  面变更带兼容负担**（版本化弃期 / CHANGELOG 记载）。
- **本版在架形态 = 执行层（memory/host/vm）之前的最后一个基线**；S6 起新包
  留存于 git，发布随执行层收官批统一裁定（见 0.6.0 档案，待发布后补写）。
