# source_hygiene 白名单：**允许入库且含真 NUL** 的文件（一行一路径 + 理由注释）。
#
# 判据：真 NUL 会让 git 判二进制（w/-text）——.gitattributes 行尾转换失效、
# diff/PR 审阅不可见（docs/** 148 行事故族 + host_test.mbt 15KB 不可见事故）。
# 源码字面量一律用转义写法（\x00）；确需入库的二进制产物在此登记。
# 当前空集——零例外是默认状态，新增条目必须附理由。
