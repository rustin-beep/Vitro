# vitro/engine 0.2.0（2026-09-19）

- **包清单（10）**：新增 `lexer` + 子包 `lexer/token`、`lexer/internal/{scanner,pp,host}`
  + 工具 `cmd/dump_tokens`；存量 4 包随 module 级发布同步重发。
- **主题**：**S2 词法器收官**——独立预处理 pass + LineMap + 宿主 IO；词法差分
  随机 2400 例（4800 TSV）+ 真实语料 444 例逐字节一致。
- **版本语义**：minor（新增包）。
