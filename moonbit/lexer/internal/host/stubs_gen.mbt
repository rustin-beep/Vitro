// Generated by moonbit/scripts/gen_stubs from native/runtime_libc/include/*.h — DO NOT EDIT
// source sha256: 9df81a02a5ec8b415ca632252357217f662a9f1626ac94bf70e98f522db221bd（14 存根规范化行尾后按固定顺序拼接）

///|
/// 标准库存根内容表（14 个；内容唯一来源 = Rust oracle 冻结资产，本文件为生成物）。
pub fn stub_lookup(name : String) -> String? {
  match name {
    "stdio.h" =>
      Some(
        "typedef unsigned int size_t;\ntypedef void* FILE;\n\nint printf(const char* fmt);\nint scanf(const char* fmt);\nint sprintf(char* buf, const char* fmt);\nint snprintf(char* buf, int size, const char* fmt);\nint sscanf(const char* str, const char* fmt);\nint fprintf(FILE* stream, const char* fmt);\nint getchar(void);\nint putchar(int c);\nint puts(const char* s);\nFILE* fopen(const char* path, const char* mode);\nint fclose(FILE* stream);\nint fread(void* ptr, int size, int nmemb, FILE* stream);\nint fwrite(void* ptr, int size, int nmemb, FILE* stream);\nint feof(FILE* stream);\nchar* fgets(char* s, int n, FILE* stream);\nint fputs(const char* s, FILE* stream);\nint ungetc(int c, FILE* stream);\nint fgetc(FILE* stream);\nint fputc(int c, FILE* stream);\nint fseek(FILE* stream, int offset, int whence);\nint ftell(FILE* stream);\nvoid rewind(FILE* stream);\nint fflush(FILE* stream);\nvoid perror(const char* s);\nvoid clearerr(FILE* stream);\nint remove(const char* pathname);\nint rename(const char* oldpath, const char* newpath);\n",
      )
    "stdlib.h" =>
      Some(
        "typedef unsigned int size_t;\n\nvoid* malloc(int size);\nvoid* calloc(int nmemb, int size);\nvoid free(void* ptr);\nvoid* realloc(void* ptr, int size);\nint atoi(const char* s);\ndouble atof(const char* s);\nlong long atol(const char* s);\nint abs(int n);\nint rand(void);\nvoid srand(int seed);\nvoid qsort(void* base, int nmemb, int size, int (*compar)(const void*, const void*));\nvoid* bsearch(const void* key, const void* base, int nmemb, int size, int (*compar)(const void*, const void*));\nvoid exit(int status);\nvoid abort(void);\nlong long strtol(const char* str, char** endptr, int base);\ndouble strtod(const char* str, char** endptr);\nlong long llabs(long long n);\nlong long llabs(long long n);\n",
      )
    "ctype.h" =>
      Some(
        "int isdigit(int c);\nint isalpha(int c);\nint islower(int c);\nint isupper(int c);\nint tolower(int c);\nint toupper(int c);\nint isspace(int c);\nint isalnum(int c);\nint isprint(int c);\nint iscntrl(int c);\nint isxdigit(int c);\nint isgraph(int c);\nint ispunct(int c);\nint isblank(int c);\n",
      )
    "math.h" =>
      Some(
        "double sin(double x);\ndouble cos(double x);\ndouble tan(double x);\ndouble sqrt(double x);\ndouble pow(double x, double y);\ndouble atan(double x);\ndouble log(double x);\ndouble log10(double x);\ndouble exp(double x);\ndouble fabs(double x);\ndouble ceil(double x);\ndouble floor(double x);\ndouble round(double x);\ndouble fmod(double x, double y);\ndouble asin(double x);\ndouble acos(double x);\ndouble atan2(double y, double x);\ndouble sinh(double x);\ndouble cosh(double x);\ndouble tanh(double x);\n",
      )
    "string.h" =>
      Some(
        "typedef unsigned int size_t;\n\nint strlen(const char* s);\nchar* strcpy(char* dest, const char* src);\nchar* strncpy(char* dest, const char* src, int n);\nint strcmp(const char* s1, const char* s2);\nint strncmp(const char* s1, const char* s2, int n);\nchar* strcat(char* dest, const char* src);\nchar* strncat(char* dest, const char* src, int n);\nvoid* memcpy(void* dest, const void* src, int n);\nvoid* memmove(void* dest, const void* src, int n);\nvoid* memset(void* ptr, int value, int n);\nint memcmp(const void* s1, const void* s2, int n);\nchar* strchr(const char* s, int c);\nchar* strrchr(const char* s, int c);\nchar* strstr(const char* haystack, const char* needle);\nvoid* memchr(const void* s, int c, int n);\nchar* strdup(const char* s);\nchar* strerror(int errnum);\nchar* strpbrk(const char* s, const char* accept);\nint strspn(const char* s, const char* accept);\nint strcspn(const char* s, const char* reject);\n",
      )
    "stdarg.h" =>
      Some(
        "typedef char* va_list;\n\nvoid __vitro_va_start(char** ap, void* last, int last_size);\nvoid* __vitro_va_arg(char** ap, int size);\nvoid __vitro_va_end(char** ap);\nvoid __vitro_va_copy(char** dst, char** src);\n\n#define va_start(ap, last) __vitro_va_start(&(ap), &(last), sizeof(last))\n#define va_arg(ap, type) (*(type*)__vitro_va_arg(&(ap), sizeof(type)))\n#define va_end(ap) __vitro_va_end(&(ap))\n#define va_copy(dst, src) __vitro_va_copy(&(dst), &(src))\n",
      )
    "limits.h" =>
      Some("/* Vitro limits.h stub — macros predefined in lexer */\n")
    "stdbool.h" => Some("/* Vitro stdbool.h stub */\ntypedef int bool;\n")
    "stddef.h" =>
      Some(
        "/* Vitro stddef.h stub */\ntypedef unsigned int size_t;\ntypedef int ptrdiff_t;\n\n/* C23（E3）：不可达标注。执行到即教学 trap；死代码中的调用无副作用。 */\nvoid unreachable(void);\n",
      )
    "stdint.h" =>
      Some(
        "/* Vitro stdint.h stub */\ntypedef char int8_t;\ntypedef unsigned char uint8_t;\ntypedef short int16_t;\ntypedef unsigned short uint16_t;\ntypedef int int32_t;\ntypedef unsigned int uint32_t;\ntypedef long long int64_t;\ntypedef unsigned long long uint64_t;\n",
      )
    "time.h" =>
      Some(
        "/* Vitro time.h stub */\ntypedef long long time_t;\ntypedef long long clock_t;\n\n#define CLOCKS_PER_SEC 1000000\n\ntime_t time(time_t* tloc);\nclock_t clock(void);\n",
      )
    "assert.h" =>
      Some(
        "/* Vitro assert.h stub */\nvoid __vitro_assert_fail(void);\n#define assert(expr) if (!(expr)) __vitro_assert_fail()\n",
      )
    "errno.h" =>
      Some(
        "/* Vitro errno.h stub */\nextern int errno;\n\n#define EINVAL 1\n#define ERANGE 2\n#define EDOM 3\n#define ENOENT 4\n#define EACCES 5\n",
      )
    "float.h" =>
      Some(
        "/* Vitro float.h stub */\n#define FLT_RADIX 2\n#define FLT_DIG 6\n#define DBL_DIG 15\n#define FLT_MIN_EXP (-125)\n#define DBL_MIN_EXP (-1021)\n#define FLT_MAX_EXP 128\n#define DBL_MAX_EXP 1024\n#define FLT_MIN 1.17549435e-38F\n#define DBL_MIN 2.2250738585072014e-308\n#define FLT_MAX 3.40282347e+38F\n#define DBL_MAX 1.7976931348623157e+308\n#define FLT_EPSILON 1.19209290e-7F\n#define DBL_EPSILON 2.2204460492503131e-16\n",
      )
    _ => None
  }
}
